{
    "rules": [
        {
            "action": "block",
            "pattern": "useGrimAdapter"
        },
        {
            "action": "block",
            "pattern": "Screenshot aborted"
        },
        {
            "action": "block",
            "pattern": "Disconnected"
        }
    ]
}
